import { FilterImagePipe } from './filter-image.pipe';

describe('FilterImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterImagePipe();
    expect(pipe).toBeTruthy();
  });
});
